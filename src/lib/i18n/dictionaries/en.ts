import sk from "./sk"
import type { Dictionary } from "./sk"

// English translation is not live yet — the site ships Slovak-only for now.
// This file exists so the i18n architecture (typed dictionary, `getDictionary`)
// is ready: translating means replacing these values, not restructuring code.
const en: Dictionary = sk

export default en
